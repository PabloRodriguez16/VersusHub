export enum tournamentStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  FINISHED = 'finished',
  CANCELLED = 'cancelled',
}

export enum tournamentType {
  MEDIUM = 'medium',
}

export enum MatchStatus {
  SCHEDULED = 'scheduled',
  INPROGRESS = 'inProgress',
  COMPLETED = 'completed',
}
